# free-match board env (copy to .env if you use a loader; server also reads process env)

# Bind address
FM_HOST=0.0.0.0
FM_PORT=8787

# Message store directory (dumb pipe filesystem)
# FM_BOARD_DIR=runtime/board/messages
